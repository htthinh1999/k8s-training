apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami-ingress
spec:
  ingressClassName: public
  rules:
    - host: whoami.microk8s.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: whoami
                port:
                  number: 80
          - path: /test
            pathType: Prefix
            backend:
              service:
                name: echo
                port:
                  number: 80
