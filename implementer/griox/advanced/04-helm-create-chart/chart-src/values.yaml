# Drop this in place of the default mychart/values.yaml

replicaCount:  2

image:
  repository:  nginx
  tag: "1.27-alpine"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

message:  "hello from griox first chart"

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 200m
    memory: 64Mi
