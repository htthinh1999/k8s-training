apiVersion: v1
kind: Service
metadata:
  name: whoami-clusterip
spec:
  type: ClusterIP
  selector:
    app: # TODO: match the Deployment's Pod label, e.g. whoami
  ports:
    - port: # TODO: port exposed by the Service, e.g. 80
      targetPort: 80
