apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:               # stringData lets you write plain text; Kubernetes base64-encodes it for you
  DB_PASSWORD: # TODO: any string, e.g. sup3rsecret
