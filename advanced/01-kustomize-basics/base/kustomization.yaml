apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - # TODO: list the manifest files kustomize should manage, e.g. deployment.yaml and service.yaml

namePrefix: # TODO: a prefix applied to every resource name, e.g. kz-

commonLabels:
  managed-by: # TODO: any label value, e.g. kustomize

configMapGenerator:
  - name: nginx-config
    literals:
      - GREETING= # TODO: any string, e.g. "hello from kustomize"
